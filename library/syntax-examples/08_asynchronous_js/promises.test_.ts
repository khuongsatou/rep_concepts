// // tests/promises.test.ts

// import { expect } from "chai";
// import { calculateSumAsync } from "./promises";

// // describe("Promises Test Suite", () => {
// // //   it("should correctly calculate the sum asynchronously using promises", () => {
// // //     //Sử dụng return để mocha biết rằng kiểm thử là bất đồng bộ
// // //     return calculateSumAsync(3, 4).then((result) => {
// // //       expect(result).to.equal(7);
// // //     });
// // //   });

// // //   it("should correctly calculate the sum asynchronously with a different timeout", () => {
// // //     // Sử dụng return để mocha biết rằng kiểm thử là bất đồng bộ
// // //     // return calculateSumAsync(2, 5).then((result) => {
// // //     //   expect(result).to.equal(7);
// // //     // });
// // //   },2000); // Tăng thời gian timeout nếu cần thiết
// // });
