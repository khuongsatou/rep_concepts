// logical_operators.ts

// Logical AND (&&)
export const andResult: boolean = true && false;

// Logical OR (||)
export const orResult: boolean = true || false;

// Logical NOT (!)
export const notResult: boolean = !true;
