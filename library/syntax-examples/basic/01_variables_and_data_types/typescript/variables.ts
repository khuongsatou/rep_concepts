// Declare variables using var, let, const
let variableVar: number = 10;
let variableLet: string = "Hello, World!";
const variableConst: boolean = true;

// Display variables
// console.log(variableVar, variableLet, variableConst);

// Reassign a variable
variableVar = 20;
// console.log(variableVar);
export {variableVar, variableLet, variableConst};